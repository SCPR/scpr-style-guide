<header class="sg-content__header">
  <div class="sg-content__container">
    <h1>Web: Base Styles</h1>
    <p>Defines styles for base elements: headings, text, icons, inputs, links, etc.</p>
  </div>
</header>

<div class="sg-content__container">
  <% blog.articles.sort_by{ |a| a.data.pattern_number.to_f }.each do |article|%>
    <% next unless article.tags.include? "base" %>
    <%= partial("_partials/component", :locals => { :article => article} ) %>
  <% end %>
</div>
